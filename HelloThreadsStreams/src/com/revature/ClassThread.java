package com.revature;

//This custom Thread extends the Thread class in order to override the run() method
//We won't use one, instead we'll use the Runnable Interface in InterfaceThread
public class ClassThread extends Thread {

    @Override
    public void run(){
        //We could have defined a custom thread functionality here...
        //But we'll use InterfaceThread instead
    }

}
